# Technology Stack

> Generated by `codebase-mapper` on 2026-02-24

## Runtime

| Technology | Version | Purpose |
| ---------- | ------- | ------- |
| Python | 3.10.x target (`.python-version` = `3.10`) | Application runtime |
| uv | Project-managed (lockfile present) | Dependency management, virtualenv, command execution |
| argparse (stdlib) | Python stdlib | CLI parsing for `yolo-ex` and `yolo-ex-platform` |

## Production Dependencies

Source of truth: `pyproject.toml` (platform markers + direct wheel URLs)

| Package | Version / Spec | Purpose |
| ------- | -------------- | ------- |
| `ultralytics` | `8.4.14` | YOLO model loading/export backend |
| `coremltools` | unpinned (`darwin` only) | CoreML export support and validation checks |
| `torch` | `2.7.0` (`darwin`) | PyTorch runtime for macOS export environment |
| `torchvision` | `0.22.0` (`darwin`) | PyTorch vision ops for macOS export environment |
| `torch` | direct Jetson wheel URL (`linux/aarch64`) | Jetson-specific PyTorch runtime |
| `torchvision` | direct Jetson wheel URL (`linux/aarch64`) | Jetson-specific torchvision runtime |
| `tensorrt-cu12-bindings` | `10.7.0.post1` (`linux/aarch64`) | TensorRT Python bindings for Jetson export |

## Development Dependencies

Source of truth: `pyproject.toml` `[dependency-groups].dev`; installed versions below are from the current `uv` environment on this machine.

| Package | Spec in `pyproject.toml` | Installed (current `.venv`) | Purpose |
| ------- | ------------------------- | --------------------------- | ------- |
| `pytest` | unpinned | `9.0.2` | Unit test runner |
| `ruff` | unpinned | `0.15.2` | Linting/import order/style checks |
| `mypy` | unpinned | `1.19.1` | Static type checking |

## Installed Runtime Snapshot (Current `uv` Environment)

Collected from `uv run --no-sync` on this machine (macOS arm64):

| Package | Installed Version | Notes |
| ------- | ----------------- | ----- |
| `ultralytics` | `8.4.14` | Matches pinned version |
| `torch` | `2.7.0` | Matches macOS expected version in platform checker |
| `torchvision` | `0.22.0` | Matches macOS expected version in platform checker |
| `coremltools` | `9.0` | Installed on macOS |

## Build and Packaging

| Tool | Config | Purpose |
| ---- | ------ | ------- |
| `uv_build` | `build-system` backend (`>=0.10.5,<0.11.0`) | Build backend |
| `tool.uv.build-backend` | `module-name = "yolo_ex"`, `module-root = "src"` | `src/` layout packaging |
| `project.scripts` | `yolo-ex`, `yolo-ex-platform` | Console script entrypoints |

## Quality Tooling

| Tool | Config | Purpose |
| ---- | ------ | ------- |
| Ruff | `target-version = "py310"`, line length `100` | Linting/import sorting |
| Mypy | `strict = true`, `python_version = "3.10"` | Type checking |
| Pytest | `testpaths = ["tests"]` | Test discovery |

## Infrastructure

| Service | Provider | Purpose |
| ------- | -------- | ------- |
| Package Index | PyPI | Default Python package resolution |
| Package Index (Jetson install flow) | NVIDIA PyPI (`https://pypi.nvidia.com/simple`) | Resolve `tensorrt-cu12-bindings` on Jetson |
| Artifact Hosting (Jetson wheels) | GitHub Releases (`ultralytics/assets`) | Jetson `torch`/`torchvision` wheel downloads |

## Configuration

| Variable / Config | Purpose | Required |
| ----------------- | ------- | -------- |
| `.python-version` (`3.10`) | Pins local Python major/minor for `uv` workflows | Yes |
| `pyproject.toml` platform markers | Select macOS vs Jetson dependencies | Yes |
| `uv sync --index https://pypi.nvidia.com/simple` (Jetson) | Adds NVIDIA index during Jetson dependency installation | Jetson only |
| `--verbose` (CLI flag) | Enables debug logging for both CLIs | No |

